'use strict';

var nomnom = require('nomnom');
var licenseModelBuilder = require('./licenseModelBuilder.js');

describe('argv', function() {
	// it('no arguments sets empty', function() {
	// 	var opts = nomnom.parse([""], ["list"]);
	// 	// var opts = argv(emptyCliArgs);
	// 	expect(opts.empty).toBe(true);
	// });

	it('', function() {

	});
});